package Objects;

public class Main {
  public static void main(String[] args) {
    /*
     * An object has state/attributes (descriptive characteristcs) and behaviors
     * (what it can do (or what can be done to it)).
     * For example, a coid can be flipped so that it's face shows either "heads" or
     * "tails", the coin's state being its current face (heads or tails) and its
     * behavior being that it can be flipped. (It should be noted that the coin's
     * behavior can change its state)
     * A class is a blueprint of an object, a model or patter from which objects are
     * created.
     * For example, the string class is used to define String objects.
     * Each String object contains specific characters (its state) and
     */
  }
}